package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Println(math.Ceil(1.60))
	fmt.Println(math.Floor(1.60))
	fmt.Println(math.Round(1.60))
	fmt.Println(math.Max(100, 90))
	fmt.Println(math.Min(900, 90))
	fmt.Println(math.Sqrt(90))
}
